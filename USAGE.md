# tdlas-mcp 用户使用指南

> 新用户接入 AI 后怎么用：这不是技术文档，是"你该说什么话"的话术手册。

---

## 一、3 分钟快速上手

接入 AI 后，直接说这三句话就能跑通：

```
1. "画一个 CH4 在 2968.5 cm⁻¹ 附近的 WMS 图"
2. "浓度 50 ppm，光程 10 cm"
3. "出图"
```

AI 会自动：选参数 → 仿真 → 画六子图 → 校验 → 给你图。

---

## 二、你能做什么（能力清单）

| 你想做什么 | 跟 AI 说什么 |
|---|---|
| 看吸收谱 | "画 CH4 @ 2968.5 cm⁻¹ 的 DAS 图" |
| 看 WMS 谐波 | "画 CH4 的 WMS 图，2f 信号" |
| 测检测限 | "算一下这个系统的 LOD" |
| 多浓度对比 | "画 10、50、100 ppm 的 WMS 对比" |
| 混合气 | "画 CH4 1% + CO2 4% 的混合气" |
| 设备选型 | "光程要多少才能测 1 ppm CH4？" |
| 反演浓度 | "这是我测的 2f 数据，帮我算浓度" |
| 稳定性分析 | "跑个 Allan 方差，测系统稳定性" |

---

## 三、12 个工具分别什么时候用

| 工具 | 什么时候用 | 你该怎么说 |
|---|---|---|
| `tdlas_wms_instrument` | **默认首选**——你要 WMS 图、要 2f/1f | "画 WMS 图" |
| `tdlas_das_instrument` | 你要看 DAS 直接吸收（无调制） | "画 DAS 图" |
| `tdlas_das_chain` | 你要看信号处理全链路（PD→基线→吸光度） | "看 DAS 信号处理流程" |
| `tdlas_simulate` | 你只要数值不要图（快速算峰高） | "算一下 2f 峰高，不用画图" |
| `tdlas_invert` | 你有真实实验数据要反演浓度 | "帮我反演浓度" |
| `tdlas_detection_limit` | 你要算 LOD/NEC | "算检测限" |
| `tdlas_detection_limit_scan` | 你要选光程/选浓度（网格扫描） | "光程选多少合适？帮我扫一下" |
| `tdlas_device` | 你要存自己的仪器（USB-6211、Thorlabs PD） | "我用的是 NI USB-6211，存一下" |
| `tdlas_session` | 你要跨会话记住参数 | "记住我这套参数，下次接着用" |
| `tdlas_guide` | 你不知道该问什么 | "告诉我怎么开始" |
| `tdlas_review` | 你要校验结果对不对 | "检查一下这个结果" |
| `tdlas_selftest` | 你不确定系统是否正常 | "跑个自检" |

---

## 四、高级功能怎么触发

### 4.1 多浓度标定曲线

```
说："画 10、50、100、500 ppm CH4 的 WMS 对比"
效果：所有浓度叠在一张图上，调制系数自动锁定，幅值可比
```

### 4.2 混合气

```
说："CH4 1% + CO2 4%，在 2 μm 附近"
效果：各组分吸收叠加，自动返回每组分归因
```

### 4.3 设备库（你的真实仪器）

```
说："我用的是 nanoplus 3373 nm DFB + NI USB-6211 + VIGO LabM-I-5"
效果：AI 自动查规格书参数 → 存进设备库 → 以后直接引用
```

### 4.4 检测限扫描（系统选型）

```
说："我要测 1 ppm CH4，光程选多少合适？"
效果：自动扫 LOD vs 光程、LOD vs 浓度，给最优值
```

### 4.5 Allan 稳定性分析

```
说："跑个 Allan 方差，看系统最优平均时间"
效果：时序图 + Allan 双对数图 + τ_opt + 噪声类型诊断
```

### 4.6 整条谱拟合反演（比单点准 5 倍）

```
说："帮我用整条 2f 谱反演浓度，不要只用峰点"
效果：最小二乘拟合，给 σ 和 χ²_red，比单点除法准
```

---

## 五、常见问题

**Q：为什么 AI 问我好多参数？**
A：它在按优先级找参数：① 你的实测值 → ② 器件型号 → ③ 默认值。说"用默认值"就跳过。

**Q：图上为什么有红色剔除区？**
A：扫描两端是电压回扫，锁相不稳定，自动剔除。不是 bug。

**Q：为什么 2f 不是标准双峰？**
A：你选的波段有很多条线叠加了。说"换个孤立单线的位置"就行。

**Q：结果能直接写论文吗？**
A：不能。这是仿真，必须标注"理论仿真"，不能冒充实验数据。

**Q：怎么知道参数用了默认值？**
A：AI 会在 `assumptions` 里列出哪些参数是默认值，你检查一下。

---

## 六、完整对话示例

```
你：画个 CH4 的 WMS 图
AI：（问：波段？浓度？光程？）
你：2968.5 cm⁻¹，50 ppm，10 cm
AI：（仿真、出图、校验）
你：光程改成 100 cm
AI：（重算）
你：再画 10、50、100 ppm 对比
AI：（多浓度叠加图）
你：算一下 LOD
AI：（LOD + 扫描图）
你：跑个 Allan
AI：（Allan 图 + τ_opt）
```

---

## 七、项目地址

- 仓库：https://github.com/LKF0402/tdlas-mcp
- HAPI：https://github.com/hitranonline/hapi
