# 运行期依赖（tdlas-mcp）
# 注意：hitran-api 是发行名，import 名是 hapi（HAPI 1.x，免 API key）。
# 装完后务必跑一次全链路自检：python tools/tdlas_mcp.py --selftest
# 另可跑离线契约自检（不需要网络）：python tools/contract_check.py
#
# 版本约束理由：本工具承诺"同输入同输出"（默认 seed=0 可复现），而 numpy 的
# 随机数流与 np.round 舍入行为跨大版本可能变化，故给上界以免静默改变数值结果。
numpy>=1.24,<3
matplotlib>=3.7,<4
scipy>=1.10,<2
hitran-api>=1.3,<2
